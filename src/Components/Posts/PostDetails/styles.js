import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
	post: {
		display: "flex",
		flexDirection: "column",
		alignItems: "center",
	},
	postDetails: {
		textAlign: "center",
	},
}));
